{
  "buildCommand": "pnpm --filter @solaudit/db exec prisma generate && pnpm --filter @solaudit/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "apps/web/src/app/api/**/*.ts": {
      "maxDuration": 10
    }
  }
}
