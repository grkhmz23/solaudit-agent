{
  "name": "@solaudit/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm --clean --out-dir dist --external @solaudit/db --external @solaudit/queue --external @solaudit/engine --external @solaudit/storage --external bullmq --external ioredis --external @prisma/client --external @aws-sdk/client-s3 --external @aws-sdk/s3-request-presigner",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@solaudit/db": "workspace:*",
    "@solaudit/queue": "workspace:*",
    "@solaudit/engine": "workspace:*",
    "@solaudit/storage": "workspace:*",
    "bullmq": "^5.12.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "tsup": "^8.4.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
